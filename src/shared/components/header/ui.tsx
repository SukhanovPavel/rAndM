import { LogoBlack } from '@/assets';

import styles from './ui.module.css';

export const Header = () => {
  return (
    <header className={styles.header}>
      <LogoBlack />
    </header>
  );
};
